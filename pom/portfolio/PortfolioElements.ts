export class PortfolioElements {
    protected static PRICE = `p:has-text('Fiyat')`;
    protected static MAX_PRICE = `input[placeholder='En Az']`;
    protected static MIN_PRICE = `input[placeholder='En Çok']`;
}